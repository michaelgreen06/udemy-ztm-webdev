const str='ztm is the best of the best'

//replaceAll is a new method we can use on all strings!
//replcae is the old method and only replaces the 1st instance of the word

str.replaceAll('best', 'worst')
